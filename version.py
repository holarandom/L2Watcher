# version.py
"""
Версия и название приложения — единый источник правды.
Меняешь здесь — меняется везде (заголовок окна, /health, About, релизы).
"""

APP_VERSION = "1.2.0"

# Название можно поменять в одном месте. Используется в заголовке окна,
# в боте, при сборке .exe.
APP_NAME = "L2 Watcher"

# Короткий машинный идентификатор. ОСТАВЛЕН как L2Monitor намеренно:
# к нему привязаны папка настроек (%APPDATA%\L2Monitor) и мьютекс.
# Менять нельзя — иначе у пользователей "потеряются" сохранённые настройки
# и обученные шаблоны. Видимое имя (APP_NAME) при этом L2 Watcher.
#
# С 1.1.0 автозагрузка к этому имени больше НЕ привязана: она переехала
# с ключа реестра Run на ярлык "L2 Watcher.lnk" в папке «Автозагрузка»
# (см. autostart.py — реестровый ключ Run считается антивирусами признаком
# закрепления вредоноса).
APP_ID = "L2Monitor"
