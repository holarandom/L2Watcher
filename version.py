# version.py
"""
Версия и название приложения — единый источник правды.
Меняешь здесь — меняется везде (заголовок окна, /health, About, релизы).
"""

APP_VERSION = "1.0.0"

# Название можно поменять в одном месте. Используется в заголовке окна,
# в боте, при сборке .exe.
APP_NAME = "L2 Watcher"

# Короткий машинный идентификатор. ОСТАВЛЕН как L2Monitor намеренно:
# к нему привязаны папка настроек (%APPDATA%\L2Monitor), ключ автозагрузки
# и мьютекс. Менять нельзя — иначе у пользователей "потеряются" сохранённые
# настройки и обученные шаблоны. Видимое имя (APP_NAME) при этом L2 Watcher.
APP_ID = "L2Monitor"
